{
  "name": "markdown-server",
  "version": "0.0.1",
  "description": "a simple server to render markdown",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node ./build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stephan.x.tabor@gsk.com",
  "private": true,
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/marked": "^1.1.0",
    "@types/node": "^14.6.2",
    "prettier": "^2.1.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "fastify": "^3.3.0",
    "marked": "^1.1.1"
  }
}
